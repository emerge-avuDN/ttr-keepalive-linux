Sends a random keypress in a time period of 1-2 minutes (inclusive) to keep your toon(s) in the game ToonTown Rewritten awake. Requires `xdotool`, which is almost certainly in your distro's repo. 

# Usage
Simply run the script in Python. 
`python keepalive.py`
